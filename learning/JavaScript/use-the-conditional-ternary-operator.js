//The conditional operator, also called the ternary operator, can be used as a one line if-else expression. condition ? statement-if-true : statement-if-false;
function checkEqual(a, b) {
return a === b ? "Equal" : "Not Equal";
}

checkEqual(1, 2);
